This repo is to store the releases of the Among NFT minter as I push them. The source code is within a private repository, but there seems
to be some permissions issues that I can't resolve, so I am making this public repo to push releases instead
